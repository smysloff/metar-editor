(function () {
  'use strict';

  function _arrayLikeToArray(r, a) {
    (null == a || a > r.length) && (a = r.length);
    for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e];
    return n;
  }
  function _arrayWithHoles(r) {
    if (Array.isArray(r)) return r;
  }
  function _assertClassBrand(e, t, n) {
    if ("function" == typeof e ? e === t : e.has(t)) return arguments.length < 3 ? t : n;
    throw new TypeError("Private element is not present on this object");
  }
  function asyncGeneratorStep(n, t, e, r, o, a, c) {
    try {
      var i = n[a](c),
        u = i.value;
    } catch (n) {
      return void e(n);
    }
    i.done ? t(u) : Promise.resolve(u).then(r, o);
  }
  function _asyncToGenerator(n) {
    return function () {
      var t = this,
        e = arguments;
      return new Promise(function (r, o) {
        var a = n.apply(t, e);
        function _next(n) {
          asyncGeneratorStep(a, r, o, _next, _throw, "next", n);
        }
        function _throw(n) {
          asyncGeneratorStep(a, r, o, _next, _throw, "throw", n);
        }
        _next(void 0);
      });
    };
  }
  function _checkPrivateRedeclaration(e, t) {
    if (t.has(e)) throw new TypeError("Cannot initialize the same private elements twice on an object");
  }
  function _classCallCheck(a, n) {
    if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function");
  }
  function _classPrivateFieldGet2(s, a) {
    return s.get(_assertClassBrand(s, a));
  }
  function _classPrivateFieldInitSpec(e, t, a) {
    _checkPrivateRedeclaration(e, t), t.set(e, a);
  }
  function _classPrivateMethodInitSpec(e, a) {
    _checkPrivateRedeclaration(e, a), a.add(e);
  }
  function _defineProperties(e, r) {
    for (var t = 0; t < r.length; t++) {
      var o = r[t];
      o.enumerable = o.enumerable || false, o.configurable = true, "value" in o && (o.writable = true), Object.defineProperty(e, _toPropertyKey(o.key), o);
    }
  }
  function _createClass(e, r, t) {
    return r && _defineProperties(e.prototype, r), Object.defineProperty(e, "prototype", {
      writable: false
    }), e;
  }
  function _createForOfIteratorHelper(r, e) {
    var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
    if (!t) {
      if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e) {
        t && (r = t);
        var n = 0,
          F = function () {};
        return {
          s: F,
          n: function () {
            return n >= r.length ? {
              done: true
            } : {
              done: false,
              value: r[n++]
            };
          },
          e: function (r) {
            throw r;
          },
          f: F
        };
      }
      throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    var o,
      a = true,
      u = false;
    return {
      s: function () {
        t = t.call(r);
      },
      n: function () {
        var r = t.next();
        return a = r.done, r;
      },
      e: function (r) {
        u = true, o = r;
      },
      f: function () {
        try {
          a || null == t.return || t.return();
        } finally {
          if (u) throw o;
        }
      }
    };
  }
  function _defineProperty(e, r, t) {
    return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, {
      value: t,
      enumerable: true,
      configurable: true,
      writable: true
    }) : e[r] = t, e;
  }
  function _iterableToArrayLimit(r, l) {
    var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
    if (null != t) {
      var e,
        n,
        i,
        u,
        a = [],
        f = true,
        o = false;
      try {
        if (i = (t = t.call(r)).next, 0 === l) ; else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0);
      } catch (r) {
        o = true, n = r;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a;
    }
  }
  function _nonIterableRest() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _regenerator() {
    /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */
    var e,
      t,
      r = "function" == typeof Symbol ? Symbol : {},
      n = r.iterator || "@@iterator",
      o = r.toStringTag || "@@toStringTag";
    function i(r, n, o, i) {
      var c = n && n.prototype instanceof Generator ? n : Generator,
        u = Object.create(c.prototype);
      return _regeneratorDefine(u, "_invoke", function (r, n, o) {
        var i,
          c,
          u,
          f = 0,
          p = o || [],
          y = false,
          G = {
            p: 0,
            n: 0,
            v: e,
            a: d,
            f: d.bind(e, 4),
            d: function (t, r) {
              return i = t, c = 0, u = e, G.n = r, a;
            }
          };
        function d(r, n) {
          for (c = r, u = n, t = 0; !y && f && !o && t < p.length; t++) {
            var o,
              i = p[t],
              d = G.p,
              l = i[2];
            r > 3 ? (o = l === n) && (u = i[(c = i[4]) ? 5 : (c = 3, 3)], i[4] = i[5] = e) : i[0] <= d && ((o = r < 2 && d < i[1]) ? (c = 0, G.v = n, G.n = i[1]) : d < l && (o = r < 3 || i[0] > n || n > l) && (i[4] = r, i[5] = n, G.n = l, c = 0));
          }
          if (o || r > 1) return a;
          throw y = true, n;
        }
        return function (o, p, l) {
          if (f > 1) throw TypeError("Generator is already running");
          for (y && 1 === p && d(p, l), c = p, u = l; (t = c < 2 ? e : u) || !y;) {
            i || (c ? c < 3 ? (c > 1 && (G.n = -1), d(c, u)) : G.n = u : G.v = u);
            try {
              if (f = 2, i) {
                if (c || (o = "next"), t = i[o]) {
                  if (!(t = t.call(i, u))) throw TypeError("iterator result is not an object");
                  if (!t.done) return t;
                  u = t.value, c < 2 && (c = 0);
                } else 1 === c && (t = i.return) && t.call(i), c < 2 && (u = TypeError("The iterator does not provide a '" + o + "' method"), c = 1);
                i = e;
              } else if ((t = (y = G.n < 0) ? u : r.call(n, G)) !== a) break;
            } catch (t) {
              i = e, c = 1, u = t;
            } finally {
              f = 1;
            }
          }
          return {
            value: t,
            done: y
          };
        };
      }(r, o, i), true), u;
    }
    var a = {};
    function Generator() {}
    function GeneratorFunction() {}
    function GeneratorFunctionPrototype() {}
    t = Object.getPrototypeOf;
    var c = [][n] ? t(t([][n]())) : (_regeneratorDefine(t = {}, n, function () {
        return this;
      }), t),
      u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c);
    function f(e) {
      return Object.setPrototypeOf ? Object.setPrototypeOf(e, GeneratorFunctionPrototype) : (e.__proto__ = GeneratorFunctionPrototype, _regeneratorDefine(e, o, "GeneratorFunction")), e.prototype = Object.create(u), e;
    }
    return GeneratorFunction.prototype = GeneratorFunctionPrototype, _regeneratorDefine(u, "constructor", GeneratorFunctionPrototype), _regeneratorDefine(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", _regeneratorDefine(GeneratorFunctionPrototype, o, "GeneratorFunction"), _regeneratorDefine(u), _regeneratorDefine(u, o, "Generator"), _regeneratorDefine(u, n, function () {
      return this;
    }), _regeneratorDefine(u, "toString", function () {
      return "[object Generator]";
    }), (_regenerator = function () {
      return {
        w: i,
        m: f
      };
    })();
  }
  function _regeneratorDefine(e, r, n, t) {
    var i = Object.defineProperty;
    try {
      i({}, "", {});
    } catch (e) {
      i = 0;
    }
    _regeneratorDefine = function (e, r, n, t) {
      function o(r, n) {
        _regeneratorDefine(e, r, function (e) {
          return this._invoke(r, n, e);
        });
      }
      r ? i ? i(e, r, {
        value: n,
        enumerable: !t,
        configurable: !t,
        writable: !t
      }) : e[r] = n : (o("next", 0), o("throw", 1), o("return", 2));
    }, _regeneratorDefine(e, r, n, t);
  }
  function _slicedToArray(r, e) {
    return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest();
  }
  function _toPrimitive(t, r) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r);
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return (String )(t);
  }
  function _toPropertyKey(t) {
    var i = _toPrimitive(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _typeof(o) {
    "@babel/helpers - typeof";

    return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) {
      return typeof o;
    } : function (o) {
      return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
    }, _typeof(o);
  }
  function _unsupportedIterableToArray(r, a) {
    if (r) {
      if ("string" == typeof r) return _arrayLikeToArray(r, a);
      var t = {}.toString.call(r).slice(8, -1);
      return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0;
    }
  }

  var _non_clickable_types = /*#__PURE__*/new WeakMap();
  var _handled_events = /*#__PURE__*/new WeakMap();
  var _default_brand = /*#__PURE__*/new WeakSet();
  // file: src/core/form_manager.mjs
  var _default = /*#__PURE__*/function () {
    function _default(selector) {
      var _this = this;
      var components = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : [];
      var componentNames = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : [];
      _classCallCheck(this, _default);
      _classPrivateMethodInitSpec(this, _default_brand);
      _classPrivateFieldInitSpec(this, _non_clickable_types, ['text', 'date', 'time', 'radio', 'checkbox']);
      _classPrivateFieldInitSpec(this, _handled_events, ['click', 'input']);
      _defineProperty(this, "root", null);
      _defineProperty(this, "output", null);
      _defineProperty(this, "elements", null);
      _defineProperty(this, "forms", {
        input: null,
        output: null
      });
      _defineProperty(this, "result", new Map());
      _defineProperty(this, "registry", new Map());
      this.root = document.querySelector(selector);
      if (!this.root) {
        console.warn("can't find a form 'root' with selector ".concat(selector));
      }
      var forms = Array.from(this.root.children).filter(function (child) {
        return child.tagName === 'FORM';
      });
      this.forms.input = forms === null || forms === void 0 ? void 0 : forms.find(function (form) {
        return form.name.match(/input/i);
      });
      this.forms.output = forms === null || forms === void 0 ? void 0 : forms.find(function (form) {
        return form.name.match(/output/i);
      });
      if (!this.forms.input) {
        console.warn("can't find 'input' form in 'root' element ".concat(selector));
      }
      if (!this.forms.output) {
        console.warn("can't find 'output' form in 'root' element ".concat(selector));
      }
      this.elements = this.forms.input.elements;
      this.output = this.forms.output.elements.output;
      _classPrivateFieldGet2(_handled_events, this).forEach(function (handled_event) {
        return _this.forms.input.addEventListener(handled_event, function (event) {
          return _assertClassBrand(_default_brand, _this, _dispatch).call(_this, event);
        });
      });
      this.loadComponents(components, componentNames);
    }
    return _createClass(_default, [{
      key: "updateResult",
      value: function updateResult() {
        _assertClassBrand(_default_brand, this, _update).call(this);
      }
    }, {
      key: "addHandler",
      value: function addHandler(name) {
        var _options$element;
        var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
        // token  - specific HTMLInput 'name' attribute
        //          or abstract group of HTMLInput elemments
        // record = { token, callbacks }
        // -------------------------------------------------
        // this.registry = Map(element, record)
        // this.result   = Map(token, value)

        _assertClassBrand(_default_brand, this, _checkHandlerArguments).call(this, name, options);
        _assertClassBrand(_default_brand, this, _createResultToken).call(this, name);
        var elements = _assertClassBrand(_default_brand, this, _getElementsByName).call(this, (_options$element = options.element) !== null && _options$element !== void 0 ? _options$element : name);
        var record = _assertClassBrand(_default_brand, this, _createRecord).call(this, name, options);
        _assertClassBrand(_default_brand, this, _writeRecordToRegistry).call(this, elements, record);
        _assertClassBrand(_default_brand, this, _handleCheckedElements).call(this, elements, options);
        _assertClassBrand(_default_brand, this, _runInitHandler).call(this, elements, options);
      }
    }, {
      key: "loadComponents",
      value: function loadComponents(list, names) {
        var _iterator = _createForOfIteratorHelper(names),
          _step;
        try {
          for (_iterator.s(); !(_step = _iterator.n()).done;) {
            var name = _step.value;
            var component = list[name];
            if (!component) {
              throw new Error("Cannot find component '".concat(name, "'"));
            }
            component(this);
          }
        } catch (err) {
          _iterator.e(err);
        } finally {
          _iterator.f();
        }
      }
    }]);
  }();
  function _dispatch(event) {
    var element = Array.from(this.elements).find(function (element) {
      return element === event.target;
    });
    if (!element) {
      if (event.target instanceof HTMLOptionElement) {
        element = event.target.parentElement;
      } else {
        return;
      }
    }
    if (event.type === 'click') {
      if (element instanceof HTMLInputElement && _classPrivateFieldGet2(_non_clickable_types, this).includes(element.type) || event.target instanceof HTMLSelectElement || event.target instanceof HTMLOptionElement) {
        return;
      }
    }
    var record = this.registry.get(element);
    if (!record) {
      return;
    }
    var name = record.name;
    var _record$callbacks = record.callbacks,
      format = _record$callbacks.format,
      auxiliary = _record$callbacks.auxiliary,
      update = _record$callbacks.update;
    if (format) {
      element.value = format.call(this, element.value, element);
    }
    if (auxiliary) {
      auxiliary.call(this, element.value, element);
    }
    if (update) {
      var value = update.call(this, element.value, element);
      this.result.set(name, value);
    }
    _assertClassBrand(_default_brand, this, _update).call(this);
  }
  function _update() {
    var result = [];
    var _iterator2 = _createForOfIteratorHelper(this.result),
      _step2;
    try {
      for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
        var _step2$value = _slicedToArray(_step2.value, 2),
          key = _step2$value[0],
          value = _step2$value[1];
        var item = value.trim();
        if (item !== '') result.push(item);
      }
    } catch (err) {
      _iterator2.e(err);
    } finally {
      _iterator2.f();
    }
    var text = result.join(' ');
    this.output instanceof HTMLInputElement ? this.output.value = text : this.output.textContent = text;
  }
  function _checkHandlerArguments(groupname, options) {
    if (typeof groupname !== 'string' || groupname === '') {
      throw new TypeError("AMTK_Form::addHandler must be a non empty string");
    }
  }
  function _createResultToken(name) {
    if (!this.result.has(name)) {
      this.result.set(name, '');
    }
  }
  function _getElementsByName(name) {
    return Array.from(this.elements).filter(function (element) {
      return element.name === name;
    });
  }
  function _createRecord(groupname, _ref) {
    var format = _ref.format,
      auxiliary = _ref.auxiliary,
      update = _ref.update;
    return {
      name: groupname,
      callbacks: {
        format: format,
        auxiliary: auxiliary,
        update: update
      }
    };
  }
  function _writeRecordToRegistry(elements, record) {
    var _this2 = this;
    elements.forEach(function (element) {
      return _this2.registry.set(element, record);
    });
  }
  function _updateResultByValueOf(element) {
    var record = this.registry.get(element);
    this.result.set(record.name, element.value);
  }
  function _handleCheckedElements(elements, _ref2) {
    var checked = _ref2.checked;
    if (!checked) {
      return;
    }
    if (typeof checked === 'string') {
      checked = [checked];
    }
    if (!Array.isArray(checked)) {
      throw new TypeError("'checked' must be 'string', 'array of strings' or 'null'");
    }
    var _iterator3 = _createForOfIteratorHelper(checked),
      _step3;
    try {
      for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
        var value = _step3.value;
        if (typeof value !== 'string') {
          throw new TypeError("'checked' must be 'string', 'array of strings' or 'null'");
        }
        var _iterator4 = _createForOfIteratorHelper(elements),
          _step4;
        try {
          for (_iterator4.s(); !(_step4 = _iterator4.n()).done;) {
            var element = _step4.value;
            if (element instanceof HTMLInputElement && ['checkbox', 'radio'].includes(element.type)) {
              if (element.value === value) {
                element.checked = true;
                _assertClassBrand(_default_brand, this, _updateResultByValueOf).call(this, element);
              }
            }
          }
        } catch (err) {
          _iterator4.e(err);
        } finally {
          _iterator4.f();
        }
      }
    } catch (err) {
      _iterator3.e(err);
    } finally {
      _iterator3.f();
    }
    _assertClassBrand(_default_brand, this, _update).call(this);
  }
  function _runInitHandler(elements, _ref3) {
    var init = _ref3.init;
    if (init) {
      init.call(this, elements);
      _assertClassBrand(_default_brand, this, _update).call(this);
    }
  }

  // file: src/app/main/main.mjs
  // type: 'SA_SEND' - METAR, 'SP_SEND' - SPECI
  function sendMetar(_x, _x2) {
    return _sendMetar.apply(this, arguments);
  }
  function _sendMetar() {
    _sendMetar = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee2(type, msgTxt) {
      var endpoint, options, response, text;
      return _regenerator().w(function (_context2) {
        while (1) switch (_context2.n) {
          case 0:
            endpoint = 'handler.php';
            options = {
              method: 'POST',
              headers: {
                'Content-Type': 'application/x-www-form-urlencoded; charset=UTF-8',
                'X-Requested-With': 'XMLHttpRequest'
              },
              body: new URLSearchParams({
                type: type,
                msgTxt: msgTxt
              }).toString()
            };
            _context2.n = 1;
            return fetch(endpoint, options);
          case 1:
            response = _context2.v;
            if (response.ok) {
              _context2.n = 2;
              break;
            }
            return _context2.a(2, {
              error: true,
              text: "\u041E\u0448\u0438\u0431\u043A\u0430: ".concat(response.status)
            });
          case 2:
            _context2.n = 3;
            return response.text();
          case 3:
            text = _context2.v;
            if (!(text !== 'ok')) {
              _context2.n = 4;
              break;
            }
            return _context2.a(2, {
              error: true,
              text: "\u041E\u0448\u0438\u0431\u043A\u0430: ".concat(text)
            });
          case 4:
            return _context2.a(2, {
              error: false,
              text: "METAR \u0432\u044B\u043F\u0443\u0449\u0435\u043D \u0443\u0441\u043F\u0435\u0448\u043D\u043E"
            });
        }
      }, _callee2);
    }));
    return _sendMetar.apply(this, arguments);
  }
  function loadForm (form) {
    form.output;
    var _form$forms$output$el = form.forms.output.elements,
      send = _form$forms$output$el.send,
      submit = _form$forms$output$el.submit,
      reset = _form$forms$output$el.reset;
    var display = form.forms.output.querySelector('#amtk_wre_metar_display');
    var send_wrapper = send.parentElement;
    var submit_wrapper = submit.parentElement;
    var display_wrapper = display.parentElement;
    var close_btn = display_wrapper.querySelector('input');
    send_wrapper.wre_hidden = false;
    submit_wrapper.wre_hidden = true;
    send.addEventListener('click', function (e) {
      e.preventDefault();
      send_wrapper.classList.add('wre_hidden');
      submit_wrapper.classList.remove('wre_hidden');
    });
    reset.addEventListener('click', function (e) {
      e.preventDefault();
      send_wrapper.classList.remove('wre_hidden');
      submit_wrapper.classList.add('wre_hidden');
    });
    close_btn.addEventListener('click', function (e) {
      display_wrapper.classList.add('wre_hidden');
      submit_wrapper.classList.add('wre_hidden');
      send_wrapper.classList.remove('wre_hidden');
    });
    form.forms.output.addEventListener('submit', /*#__PURE__*/function () {
      var _ref = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee(e) {
        var msgTxt, type, response;
        return _regenerator().w(function (_context) {
          while (1) switch (_context.n) {
            case 0:
              e.preventDefault();
              msgTxt = form.forms.output.elements.output.value;
              type = msgTxt.includes('METAR') ? 'SA_SEND' : 'SP_SEND';
              _context.n = 1;
              return sendMetar(type, msgTxt);
            case 1:
              response = _context.v;
              display.style.color = response.error ? 'red' : 'green';
              display.textContent = response.text;
              display_wrapper.classList.remove('wre_hidden');
              submit_wrapper.classList.add('wre_hidden');
            case 2:
              return _context.a(2);
          }
        }, _callee);
      }));
      return function (_x3) {
        return _ref.apply(this, arguments);
      };
    }());
  }

  // file: src/components/metar/type/type.mjs

  function update$8() {
    var result = '';
    var _iterator = _createForOfIteratorHelper(this.elements.type),
      _step;
    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        var type = _step.value;
        if (type.checked) {
          result += type.value;
          break;
        }
      }
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
    return result;
  }
  function type (form) {
    form.addHandler('type', {
      checked: 'METAR',
      update: update$8
    });
  }

  // src/core/icaos.mjs

  // Список кодов ICAO для поисковых подсказок,
  // на случай если не получилось загрузить их из БД

  var icaos = ['UEEA', 'UEEE', 'UELL', 'UERP', 'UERR', 'UESO', 'UESS', 'UEST', 'UHBB', 'UHBI', 'UHBW', 'UHHH', 'UHKK', 'UHMA', 'UHMD', 'UHMM', 'UHMP', 'UHNN', 'UHOO', 'UHPP', 'UHSS', 'UHWW', 'UIAA', 'UIBB', 'UIBS', 'UIII', 'UITT', 'UIUU', 'ULAA', 'ULAM', 'ULAS', 'ULBC', 'ULDD', 'ULKK', 'ULLI', 'ULMK', 'ULMM', 'ULNN', 'ULOL', 'ULOO', 'ULPB', 'ULSS', 'ULWU', 'ULWW', 'UMKK', 'UNAA', 'UNBB', 'UNEE', 'UNII', 'UNKL', 'UNKS', 'UNKY', 'UNNT', 'UNOO', 'UNSS', 'UNTT', 'UNWW', 'UODD', 'UOHH', 'UOII', 'URKA', 'URKG', 'URKK', 'URMG', 'URML', 'URMM', 'URMN', 'URMO', 'URMT', 'URRR', 'URRY', 'URSS', 'URWA', 'URWI', 'URWW', 'USCC', 'USCM', 'USDD', 'USHU', 'USII', 'USKK', 'USMM', 'USMU', 'USNN', 'USNR', 'USPP', 'USRK', 'USRN', 'USRO', 'USRR', 'USSS', 'USTJ', 'USTO', 'USTR', 'USUU', 'UUBA', 'UUBB', 'UUBC', 'UUBI', 'UUBK', 'UUBP', 'UUBS', 'UUBT', 'UUDD', 'UUDL', 'UUEE', 'UUEM', 'UUOB', 'UUOK', 'UUOL', 'UUOO', 'UUOR', 'UUOT', 'UUWR', 'UUWW', 'UUYH', 'UUYI', 'UUYP', 'UUYS', 'UUYW', 'UUYY', 'UWGG', 'UWKB', 'UWKD', 'UWKE', 'UWKJ', 'UWKS', 'UWLW', 'UWOO', 'UWOR', 'UWPP', 'UWPS', 'UWSB', 'UWSS', 'UWUB', 'UWUF', 'UWUK', 'UWUU', 'UWWW'];

  function disableCheckbox(name) {
    var cleanResult = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
    this.elements[name].checked = false;
    if (cleanResult) {
      this.result.set(name, '');
    }
  }
  function getFirFromPage() {
    var _element$dataset;
    var element = document.querySelector('login');
    var fir = element === null || element === void 0 || (_element$dataset = element.dataset) === null || _element$dataset === void 0 || (_element$dataset = _element$dataset.firIndex) === null || _element$dataset === void 0 ? void 0 : _element$dataset.trim().toUpperCase();
    return isValidFir(fir) ? fir : 'XXXX';
  }
  function isValidFir(fir) {
    return fir && typeof fir === 'string' && fir.length === 4 && /^[A-Z]{4}$/.test(fir);
  }
  function getIcaosFromDB(_x) {
    return _getIcaosFromDB.apply(this, arguments);
  }
  function _getIcaosFromDB() {
    _getIcaosFromDB = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee(fir) {
      var endpoint, response, data, _t;
      return _regenerator().w(function (_context) {
        while (1) switch (_context.p = _context.n) {
          case 0:
            if (isValidFir(fir)) {
              _context.n = 1;
              break;
            }
            return _context.a(2, []);
          case 1:
            endpoint = 'modules/taceditor/';
            _context.p = 2;
            _context.n = 3;
            return fetch(endpoint, {
              method: 'POST',
              headers: {
                'Content-Type': 'application/json',
                'Accept': 'application/json'
              },
              body: JSON.stringify({
                fir: fir
              })
            });
          case 3:
            response = _context.v;
            if (response.ok) {
              _context.n = 4;
              break;
            }
            throw new Error("HTTP ".concat(response.status, ": ").concat(response.statusText));
          case 4:
            _context.n = 5;
            return response.json();
          case 5:
            data = _context.v;
            if (!(data && Array.isArray(data.aerodromes))) {
              _context.n = 6;
              break;
            }
            return _context.a(2, data.aerodromes);
          case 6:
            console.warn('Unexpected response structure:', data);
            return _context.a(2, []);
          case 7:
            _context.n = 9;
            break;
          case 8:
            _context.p = 8;
            _t = _context.v;
            console.warn('Failed to fetch ICAOs for FIR', fir, ':', _t);
            return _context.a(2, []);
          case 9:
            return _context.a(2);
        }
      }, _callee, null, [[2, 8]]);
    }));
    return _getIcaosFromDB.apply(this, arguments);
  }
  function getIcaos(_x2) {
    return _getIcaos.apply(this, arguments);
  }
  function _getIcaos() {
    _getIcaos = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee2(value) {
      var fir, db_icaos, _t2;
      return _regenerator().w(function (_context2) {
        while (1) switch (_context2.p = _context2.n) {
          case 0:
            fir = getFirFromPage();
            if (!(fir === 'XXXX' && value.length === 0)) {
              _context2.n = 1;
              break;
            }
            return _context2.a(2, []);
          case 1:
            _context2.p = 1;
            _context2.n = 2;
            return getIcaosFromDB(fir);
          case 2:
            db_icaos = _context2.v;
            return _context2.a(2, db_icaos.length > 0 ? db_icaos : icaos);
          case 3:
            _context2.p = 3;
            _t2 = _context2.v;
            console.error('Error getting ICAOs:', _t2);
            return _context2.a(2, icaos);
        }
      }, _callee2, null, [[1, 3]]);
    }));
    return _getIcaos.apply(this, arguments);
  }

  function icao (form) {
    // @todo отслеживать позицию курсора при печати
    // @todo кеширование кодов ICAO из БД

    form.addHandler('icao', {
      format: format$2,
      auxiliary: updateAssumptions,
      update: update$7
    });
    form.addHandler('icao', {
      element: 'icao_variants',
      init: updateAssumptions,
      auxiliary: updateValue,
      update: update$7
    });
  }

  // Main Functions //

  function format$2(value) {
    return value.trim().toUpperCase().replace(/[^A-Z]/g, '').substring(0, 4);
  }
  function updateAssumptions() {
    return _updateAssumptions.apply(this, arguments);
  }
  function _updateAssumptions() {
    _updateAssumptions = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee() {
      var icao, variants, value, icaos, isMatchesFounded, _iterator, _step, icaoCode, option, _option, _t;
      return _regenerator().w(function (_context) {
        while (1) switch (_context.p = _context.n) {
          case 0:
            icao = this.elements.icao;
            variants = this.elements.icao_variants;
            value = icao.value.trim().toUpperCase();
            if (!(value.length === 0)) {
              _context.n = 1;
              break;
            }
            clearAll.call(this);
            return _context.a(2);
          case 1:
            clearVariants.call(this);
            _context.p = 2;
            _context.n = 3;
            return getIcaos(value);
          case 3:
            icaos = _context.v;
            isMatchesFounded = false;
            _iterator = _createForOfIteratorHelper(icaos);
            try {
              for (_iterator.s(); !(_step = _iterator.n()).done;) {
                icaoCode = _step.value;
                if (icaoCode.startsWith(value)) {
                  isMatchesFounded = true;
                  option = document.createElement('option');
                  option.value = icaoCode;
                  option.textContent = icaoCode;
                  variants.add(option);
                }
              }
            } catch (err) {
              _iterator.e(err);
            } finally {
              _iterator.f();
            }
            if (!isMatchesFounded) {
              clearVariants.call(this, true);
            }
            _context.n = 5;
            break;
          case 4:
            _context.p = 4;
            _t = _context.v;
            console.error('Error updating ICAO assumptions:', _t);
            if (variants.options.length > 0) {
              variants.options[0].textContent = 'Ошибка загрузки';
            } else {
              _option = document.createElement('option');
              _option.textContent = 'Ошибка загрузки';
              _option.disabled = true;
              _option.selected = true;
              variants.add(_option);
            }
          case 5:
            return _context.a(2);
        }
      }, _callee, this, [[2, 4]]);
    }));
    return _updateAssumptions.apply(this, arguments);
  }
  function update$7() {
    var icao = this.elements.icao;
    var value = icao.value.trim().toUpperCase();
    return value.length === 4 ? value : '';
  }
  function updateValue(value) {
    var icao = this.elements.icao;
    icao.value = value;
  }

  // Util Functions //

  function clearIcao() {
    var icao = this.elements.icao;
    icao.value = '';
  }
  function clearVariants() {
    var addDefaultOption = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
    var variants = this.elements.icao_variants;
    variants.options.length = 0;
    if (addDefaultOption) {
      var option = document.createElement('option');
      option.textContent = 'Не найдено';
      option.disabled = true;
      option.selected = true;
      variants.add(option);
    }
  }
  function clearAll() {
    clearIcao.call(this);
    clearVariants.call(this, true);
  }

  // file: src/components/metar/cor/cor.mjs

  function update$6(value, element) {
    return element.checked ? value : '';
  }
  function cor (form) {
    form.addHandler('cor', {
      update: update$6
    });
  }

  // file: src/components/metar/datetime/datetime.mjs

  function update$5() {
    var _date$value, _time$value;
    var _this$elements = this.elements,
      date = _this$elements.date,
      time = _this$elements.time;
    var dateval = (_date$value = date.value) === null || _date$value === void 0 || (_date$value = _date$value.split('-')) === null || _date$value === void 0 ? void 0 : _date$value.at(-1);
    var timeval = (_time$value = time.value) === null || _time$value === void 0 || (_time$value = _time$value.split(':')) === null || _time$value === void 0 ? void 0 : _time$value.join('');
    return dateval && timeval ? "".concat(dateval).concat(timeval, "Z") : '';
  }
  function auxiliary$3() {
    var _this$elements2 = this.elements,
      date = _this$elements2.date,
      time = _this$elements2.time;
    var now = new Date();
    var y = now.getUTCFullYear();
    var m = (now.getUTCMonth() + 1).toString().padStart(2, '0');
    var d = now.getUTCDate().toString().padStart(2, '0');
    var h = now.getUTCHours().toString().padStart(2, '0');
    var i = now.getUTCMinutes().toString().padStart(2, '0');
    date.value = "".concat(y, "-").concat(m, "-").concat(d);
    time.value = "".concat(h, ":").concat(i);
  }
  function datetime (form) {
    form.addHandler('datetime', {
      element: 'date',
      update: update$5
    });
    form.addHandler('datetime', {
      element: 'time',
      update: update$5
    });
    form.addHandler('datetime', {
      element: 'current',
      auxiliary: auxiliary$3,
      update: update$5
    });
  }

  // file: src/components/metar/nil/nil.mjs

  function auxiliary$2(value, element) {
    if (element.checked) {
      disableCheckbox.call(this, 'cor', true);
      disableCheckbox.call(this, 'auto', true);
    }
  }
  function update$4(value) {
    var nil = this.elements.nil;
    return nil.checked ? value : '';
  }
  function nil (form) {
    form.addHandler('nil', {
      auxiliary: auxiliary$2,
      update: update$4
    });
  }

  // file: src/components/metar/auto/auto.mjs

  function auxiliary$1(value, element) {
    if (element.checked) {
      disableCheckbox.call(this, 'nil', true);
    }
  }
  function update$3(value) {
    var auto = this.elements.auto;
    return auto.checked ? value : '';
  }
  function auto (form) {
    form.addHandler('auto', {
      auxiliary: auxiliary$1,
      update: update$3
    });
  }

  var round = Math.round;
  function wind (form) {
    form.addHandler('wind', {
      element: 'units',
      init: init$1,
      update: update$2
    });
    form.addHandler('wind', {
      element: 'calm',
      auxiliary: calmHandler,
      update: update$2
    });
    form.addHandler('wind', {
      element: 'vrb',
      auxiliary: vrbHandler,
      update: update$2
    });
    form.addHandler('wind', {
      element: 'direction',
      format: format$1,
      auxiliary: auxiliary,
      update: update$2
    });
    form.addHandler('wind', {
      element: 'speed',
      format: format$1,
      auxiliary: auxiliary,
      update: update$2
    });
    form.addHandler('wind', {
      element: 'gust',
      format: format$1,
      auxiliary: auxiliary,
      update: update$2
    });
    form.addHandler('wind', {
      element: 'direction_range',
      auxiliary: auxiliary,
      update: update$2
    });
    form.addHandler('wind', {
      element: 'speed_range',
      auxiliary: auxiliary,
      update: update$2
    });
    form.addHandler('wind', {
      element: 'gust_range',
      auxiliary: auxiliary,
      update: update$2
    });
    form.addHandler('wind', {
      element: 'vrb_min',
      format: format$1,
      auxiliary: auxiliary,
      update: update$2
    });
    form.addHandler('wind', {
      element: 'vrb_max',
      format: format$1,
      auxiliary: auxiliary,
      update: update$2
    });
    form.addHandler('wind', {
      element: 'vrb_min_range',
      auxiliary: auxiliary,
      update: update$2
    });
    form.addHandler('wind', {
      element: 'vrb_max_range',
      auxiliary: auxiliary,
      update: update$2
    });
  }
  function init$1(elements) {
    var _iterator = _createForOfIteratorHelper(elements),
      _step;
    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        var element = _step.value;
        element.checked = element.value === 'MPS';
      }
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
  }
  function calmHandler(value, element) {
    var vrb = this.elements.vrb;
    if (element.checked) {
      vrb.checked = false;
    }
  }
  function vrbHandler(value, element) {
    var calm = this.elements.calm;
    if (element.checked) {
      calm.checked = false;
    }
  }
  function format$1(value, element) {
    var _this$elements = this.elements,
      direction = _this$elements.direction,
      vrb_min = _this$elements.vrb_min,
      vrb_max = _this$elements.vrb_max;
    var _this$elements2 = this.elements,
      speed = _this$elements2.speed,
      gust = _this$elements2.gust;
    var directions = [direction, vrb_min, vrb_max];
    var speeds = [speed, gust];
    var trimmed = value.trim().replace(/\D/g, '');
    if (directions.includes(element)) {
      trimmed = trimmed.substring(0, 3);
      return Number(trimmed) > 360 ? 360 : trimmed;
    }
    if (speeds.includes(element)) {
      trimmed = trimmed.substring(0, 2);
      return trimmed;
    }
    return '';
  }
  function auxiliary(value, element) {
    var _this$elements3 = this.elements,
      direction = _this$elements3.direction,
      direction_range = _this$elements3.direction_range;
    var _this$elements4 = this.elements,
      speed = _this$elements4.speed,
      speed_range = _this$elements4.speed_range;
    var _this$elements5 = this.elements,
      gust = _this$elements5.gust,
      gust_range = _this$elements5.gust_range;
    var _this$elements6 = this.elements,
      vrb_min = _this$elements6.vrb_min,
      vrb_min_range = _this$elements6.vrb_min_range;
    var _this$elements7 = this.elements,
      vrb_max = _this$elements7.vrb_max,
      vrb_max_range = _this$elements7.vrb_max_range;
    if (element === direction) {
      direction_range.value = value === '' ? -10 : value;
      return;
    }
    if (element === speed) {
      speed_range.value = value === '' ? -1 : value;
      return;
    }
    if (element === gust) {
      gust_range.value = value === '' ? -1 : value;
      return;
    }
    if (element === vrb_min) {
      vrb_min_range.value = value === '' ? -10 : value;
      return;
    }
    if (element === vrb_max) {
      vrb_max_range.value = value === '' ? -10 : value;
      return;
    }
    value = Number(value);
    if (element === direction_range) {
      direction.value = value === -10 ? '' : round(value / 10) * 10;
      return;
    }
    if (element === vrb_min_range) {
      vrb_min.value = value === -10 ? '' : round(value / 10) * 10;
      return;
    }
    if (element === vrb_max_range) {
      vrb_max.value = value === -10 ? '' : round(value / 10) * 10;
      return;
    }
    if (element === speed_range) {
      speed.value = value === -1 ? '' : value;
      return;
    }
    if (element === gust_range) {
      gust.value = value === -1 ? '' : value;
      return;
    }
  }
  function update$2(value, element) {
    var _this$elements8 = this.elements,
      units = _this$elements8.units,
      calm = _this$elements8.calm,
      vrb = _this$elements8.vrb;
    var _this$elements9 = this.elements,
      direction = _this$elements9.direction;
      _this$elements9.direction_range;
    var _this$elements0 = this.elements,
      speed = _this$elements0.speed;
      _this$elements0.speed_range;
    var _this$elements1 = this.elements,
      gust = _this$elements1.gust;
      _this$elements1.gust_range;
    var _this$elements10 = this.elements,
      vrb_min = _this$elements10.vrb_min;
      _this$elements10.vrb_min_range;
    var _this$elements11 = this.elements,
      vrb_max = _this$elements11.vrb_max;
      _this$elements11.vrb_max_range;
    var result = '';
    if (calm.checked) {
      return calm.value;
    }
    if (speed.value === '') {
      return result;
    }
    if (vrb.checked && (Number(speed.value) <= 1 && units.value === 'MPS' || Number(speed.value) <= 3 && units.value === 'KT')) {
      result += vrb.value;
    } else {
      result += direction.value.padStart(3, '0');
    }
    if (Number(speed.value) >= 49 && units.value === 'MPS' || Number(speed.value) >= 99 && units.value === 'KT') {
      result += 'P';
    }
    result += speed.value.padStart(2, '0');
    if (gust.value > speed.value) {
      result += 'G';
      if (Number(gust.value) >= 49 && units.value === 'MPS' || Number(gust.value) >= 99 && units.value === 'KT') {
        result += 'P';
      }
      result += gust.value.padStart(2, '0');
    }
    result += units.value;
    if (vrb.checked && vrb_min.value !== '' && vrb_max.value !== '' && Number(vrb_max.value) > Number(vrb_min.value)) {
      var vrb_min_val = vrb_min.value.padStart(3, '0');
      var vrb_max_val = vrb_max.value.padStart(3, '0');
      result += " ".concat(vrb_min_val, "V").concat(vrb_max_val);
    }
    return result;
  }

  // file: src/components/metar/cavok/cavok.mjs

  function cavok (form) {
    form.addHandler('cavok', {
      element: 'cavok',
      auxiliary: function auxiliary(value, element) {
        var result = this.result;
        var _this$elements = this.elements,
          visibility = _this$elements.visibility,
          visibility_range = _this$elements.visibility_range;
        var _this$elements2 = this.elements,
          visibility_minimal = _this$elements2.visibility_minimal,
          visibility_minimal_range = _this$elements2.visibility_minimal_range,
          visibility_minimal_direction = _this$elements2.visibility_minimal_direction;
        var _this$elements3 = this.elements,
          visibility_vertical = _this$elements3.visibility_vertical,
          visibility_vertical_range = _this$elements3.visibility_vertical_range;
        var _this$elements4 = this.elements,
          weather = _this$elements4.weather,
          intensity = _this$elements4.intensity,
          precipitation = _this$elements4.precipitation,
          phenomena = _this$elements4.phenomena;
        var _this$elements5 = this.elements,
          clouds_1_coverage = _this$elements5.clouds_1_coverage,
          clouds_1 = _this$elements5.clouds_1,
          clouds_1_type = _this$elements5.clouds_1_type,
          clouds_1_height = _this$elements5.clouds_1_height;
        var _this$elements6 = this.elements,
          clouds_2_coverage = _this$elements6.clouds_2_coverage,
          clouds_2 = _this$elements6.clouds_2,
          clouds_2_type = _this$elements6.clouds_2_type,
          clouds_2_height = _this$elements6.clouds_2_height;
        var _this$elements7 = this.elements,
          clouds_3_coverage = _this$elements7.clouds_3_coverage,
          clouds_3 = _this$elements7.clouds_3,
          clouds_3_type = _this$elements7.clouds_3_type,
          clouds_3_height = _this$elements7.clouds_3_height;
        var _this$elements8 = this.elements,
          clouds_4_coverage = _this$elements8.clouds_4_coverage,
          clouds_4 = _this$elements8.clouds_4,
          clouds_4_type = _this$elements8.clouds_4_type,
          clouds_4_height = _this$elements8.clouds_4_height;
        if (element.checked) {
          visibility.value = '';
          visibility_range.value = -1;
          visibility_minimal.value = '';
          visibility_minimal_range.value = -1;
          visibility_vertical.value = '';
          visibility_vertical_range.value = -1;
          weather.value = '';
          intensity.forEach(function (item) {
            return item.checked = false;
          });
          precipitation.value = '';
          phenomena.value = '';
          clouds_1_coverage.value = 'NCD';
          clouds_1.value = '';
          clouds_1_type.forEach(function (type) {
            return type.checked = false;
          });
          clouds_1_height.value = -1;
          clouds_2_coverage.value = 'NCD';
          clouds_2.value = '';
          clouds_2_type.forEach(function (type) {
            return type.checked = false;
          });
          clouds_2_height.value = -1;
          clouds_3_coverage.value = 'NCD';
          clouds_3.value = '';
          clouds_3_type.forEach(function (type) {
            return type.checked = false;
          });
          clouds_3_height.value = -1;
          clouds_4_coverage.value = 'NCD';
          clouds_4.value = '';
          clouds_4_type.forEach(function (type) {
            return type.checked = false;
          });
          clouds_4_height.value = -1;
          visibility.disabled = true;
          visibility_range.disabled = true;
          visibility_minimal.disabled = true;
          visibility_minimal_range.disabled = true;
          visibility_minimal_direction.disabled = true;
          visibility_vertical.disabled = true;
          visibility_vertical_range.disabled = true;
          weather.disabled = true;
          intensity.forEach(function (item) {
            return item.disabled = true;
          });
          precipitation.disabled = true;
          phenomena.disabled = true;
          clouds_1_coverage.disabled = true;
          clouds_1.disabled = true;
          clouds_1_type.forEach(function (type) {
            return type.disabled = true;
          });
          clouds_1_height.disabled = true;
          clouds_2_coverage.disabled = true;
          clouds_2.disabled = true;
          clouds_2_type.forEach(function (type) {
            return type.disabled = true;
          });
          clouds_2_height.disabled = true;
          clouds_3_coverage.disabled = true;
          clouds_3.disabled = true;
          clouds_3_type.forEach(function (type) {
            return type.disabled = true;
          });
          clouds_3_height.disabled = true;
          clouds_4_coverage.disabled = true;
          clouds_4.disabled = true;
          clouds_4_type.forEach(function (type) {
            return type.disabled = true;
          });
          clouds_4_height.disabled = true;
          result.set('visibility', '');
          result.set('visibility_minimal', '');
          result.set('visibility_vertical', '');
          result.set('weather', '');
          result.set('clouds_1', '');
          result.set('clouds_2', '');
          result.set('clouds_3', '');
          result.set('clouds_4', '');
        } else {
          visibility.disabled = false;
          visibility_range.disabled = false;
          visibility_minimal.disabled = false;
          visibility_minimal_range.disabled = false;
          visibility_minimal_direction.disabled = false;
          visibility_vertical.disabled = false;
          visibility_vertical_range.disabled = false;
          weather.disabled = false;
          intensity.forEach(function (item) {
            return item.disabled = false;
          });
          precipitation.disabled = false;
          phenomena.disabled = false;
          clouds_1_coverage.disabled = false;
          clouds_1.disabled = false;
          clouds_1_type.forEach(function (type) {
            return type.disabled = false;
          });
          clouds_1_height.disabled = false;
          clouds_2_coverage.disabled = false;
          clouds_2.disabled = false;
          clouds_2_type.forEach(function (type) {
            return type.disabled = false;
          });
          clouds_2_height.disabled = false;
          clouds_3_coverage.disabled = false;
          clouds_3.disabled = false;
          clouds_3_type.forEach(function (type) {
            return type.disabled = false;
          });
          clouds_3_height.disabled = false;
          clouds_4_coverage.disabled = false;
          clouds_4.disabled = false;
          clouds_4_type.forEach(function (type) {
            return type.disabled = false;
          });
          clouds_4_height.disabled = false;
        }
      },
      update: function update(value, element) {
        return element.checked ? element.value : '';
      }
    });
  }

  // file: src/components/metar/visibility/visibility.mjs

  // Допустимые значения видимости

  var visibilities$1 = [-1];
  for (var i$1 = 0; i$1 < 800; i$1 += 50) visibilities$1.push(i$1);
  for (var _i$1 = 800; _i$1 < 5000; _i$1 += 100) visibilities$1.push(_i$1);
  for (var _i2$1 = 5000; _i2$1 < 10000; _i2$1 += 1000) visibilities$1.push(_i2$1);
  visibilities$1.push(9999);
  var getBase$1 = function getBase(value) {
    return value < 800 ? 50 : value < 5000 ? 100 : value < 9999 ? 1000 : 0;
  };
  function updateVisibility$2() {
    var result = this.result;
    var visibility = this.elements.visibility.value;
    var minimal = this.elements.visibility_minimal.value;
    var direction = this.elements.visibility_minimal_direction.value;
    if (minimal === '' || Number(minimal) >= Number(visibility)) {
      result.set('visibility_minimal', '');
    } else {
      result.set('visibility_minimal', minimal.padStart(4, '0') + direction);
    }
    return visibility === '' ? '' : visibility.padStart(4, '0');
  }
  function visibility (form) {
    form.addHandler('visibility', {
      format: function format(value) {
        return value.trim().replace(/\D/, '').substring(0, 4);
      },
      auxiliary: function auxiliary(value) {
        var visibility_range = this.elements.visibility_range;
        if (value === '') visibility_range.value = -1;
        var visibilityValue = Number(value);
        var base = getBase$1(visibilityValue);
        var visibilityRangeValue = visibilities$1.at(-1);
        if (base > 0) {
          var roundedValue = Math.floor(visibilityValue / base) * base;
          visibilityRangeValue = visibilities$1.indexOf(roundedValue);
        }
        visibility_range.value = visibilityRangeValue.toString();
      },
      update: updateVisibility$2
    });
    form.addHandler('visibility', {
      element: 'visibility_range',
      init: function init(elements) {
        var element = elements[0];
        element.setAttribute('min', 0);
        element.setAttribute('max', visibilities$1.length - 1);
        element.value = visibilities$1[0];
      },
      auxiliary: function auxiliary(value) {
        value = +value;
        var visibility = this.elements.visibility;
        visibility.value = value === 0 ? '' : visibilities$1[value];
      },
      update: updateVisibility$2
    });
  }

  // file: src/components/metar/visibility/minimal_visibility.mjs

  // @todo visibility_minimal_direction

  var visibilities = [-1];
  for (var i = 0; i < 800; i += 50) visibilities.push(i);
  for (var _i = 800; _i < 5000; _i += 100) visibilities.push(_i);
  for (var _i2 = 5000; _i2 < 10000; _i2 += 1000) visibilities.push(_i2);
  visibilities.push(9999);
  var getBase = function getBase(value) {
    return value < 800 ? 50 : value < 5000 ? 100 : value < 9999 ? 1000 : 0;
  };
  function updateVisibility$1() {
    var visibility = this.elements.visibility.value;
    var minimal = this.elements.visibility_minimal.value;
    var direction = this.elements.visibility_minimal_direction.value;
    return minimal === '' || Number(minimal) >= Number(visibility) ? '' : minimal.padStart(4, '0') + direction;
  }
  function visibility_minimal (form) {
    form.addHandler('visibility_minimal', {
      format: function format(value) {
        return value.trim().replace(/\D/, '').substring(0, 4);
      },
      auxiliary: function auxiliary(value) {
        var visibility_range = this.elements.visibility_minimal_range;
        if (value === '') visibility_range.value = -1;
        var visibilityValue = Number(value);
        var base = getBase(visibilityValue);
        var visibilityRangeValue = visibilities.at(-1);
        if (base > 0) {
          var roundedValue = Math.floor(visibilityValue / base) * base;
          visibilityRangeValue = visibilities.indexOf(roundedValue);
        }
        visibility_range.value = visibilityRangeValue.toString();
      },
      update: updateVisibility$1
    });
    form.addHandler('visibility_minimal', {
      element: 'visibility_minimal_range',
      init: function init(elements) {
        var element = elements[0];
        element.setAttribute('min', 0);
        element.setAttribute('max', visibilities.length - 1);
        element.value = visibilities[0];
      },
      auxiliary: function auxiliary(value) {
        value = +value;
        var visibility = this.elements.visibility_minimal;
        visibility.value = value === 0 ? '' : visibilities[value];
      },
      update: updateVisibility$1
    });
    form.addHandler('visibility_minimal', {
      element: 'visibility_minimal_direction',
      update: updateVisibility$1
    });
  }

  // file: src/components/metar/weather/weather.mjs

  function auxiliaryPhenomena(value, element) {
    var weather = this.elements.weather;
    if (value !== '') weather.value += value;
  }
  function updateWeather() {
    var weather = this.elements.weather;
    var value = weather.value.trim();
    return value.length > 1 ? value : '';
  }
  function weather (form) {
    form.addHandler('weather', {
      element: 'weather',
      format: function format(value) {
        return value.toUpperCase().replace(/[^-+A-Z ]/, '');
      },
      auxiliary: function auxiliary(value) {
        var intensity = this.elements.intensity;
        var sign = value[0];
        var _iterator = _createForOfIteratorHelper(intensity),
          _step;
        try {
          for (_iterator.s(); !(_step = _iterator.n()).done;) {
            var item = _step.value;
            item.checked = item.value === sign;
          }
        } catch (err) {
          _iterator.e(err);
        } finally {
          _iterator.f();
        }
      },
      update: updateWeather
    });
    form.addHandler('weather', {
      element: 'intensity',
      auxiliary: function auxiliary(value, element) {
        var sign = '';
        var _this$elements = this.elements,
          intensity = _this$elements.intensity,
          weather = _this$elements.weather;
        var _iterator2 = _createForOfIteratorHelper(intensity),
          _step2;
        try {
          for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
            var item = _step2.value;
            if (item !== element) item.checked = false;
            if (item.checked) sign = item.value;
          }
        } catch (err) {
          _iterator2.e(err);
        } finally {
          _iterator2.f();
        }
        weather.value = weather.value.replace(/^[+-]/, '');
        if (sign) {
          weather.value = sign + weather.value;
        }
      },
      update: updateWeather
    });
    form.addHandler('weather', {
      element: 'precipitation',
      auxiliary: auxiliaryPhenomena,
      update: updateWeather
    });
    form.addHandler('weather', {
      element: 'phenomena',
      auxiliary: auxiliaryPhenomena,
      update: updateWeather
    });
  }

  // file: src/components/metar/visibility_vertical.mjs

  function updateVisibility() {
    var visibility = this.elements.visibility_vertical.value;
    if (visibility === '') {
      return '';
    }
    if (visibility.startsWith('/') && visibility !== '///') {
      return '';
    }
    return 'VV' + visibility.padStart(3, '0');
  }
  function visibility_vertical (form) {
    form.addHandler('visibility_vertical', {
      format: function format(value) {
        value = value.trim();
        if (value.startsWith('/')) {
          return value.replace(/[^/]/g, '').substring(0, 3);
        }
        if (/^\d/.test(value)) {
          return value.replace(/\D/g, '').substring(0, 2);
        }
        return value;
      },
      auxiliary: function auxiliary(value) {
        var visibility_range = this.elements.visibility_vertical_range;
        if (value === '') {
          visibility_range.value = -1;
        } else if (value === '///') {
          visibility_range.value = 0;
        } else if (value.startsWith('/')) {
          visibility_range.value = -1;
        } else {
          visibility_range.value = value.toString();
        }
      },
      update: updateVisibility
    });
    form.addHandler('visibility_vertical', {
      element: 'visibility_vertical_range',
      init: function init(elements) {
        var element = elements[0];
        element.setAttribute('min', -1);
        element.setAttribute('max', 20);
        element.setAttribute('step', 1);
        element.value = -1;
      },
      auxiliary: function auxiliary(value) {
        value = +value;
        var visibility = this.elements.visibility_vertical;
        if (value === -1) {
          visibility.value = '';
        } else if (value === 0) {
          visibility.value = '///';
        } else {
          visibility.value = value;
        }
      },
      update: updateVisibility
    });
  }

  // file: src/components/metar/clouds/clouds.mjs

  function formatClouds(value) {
    var val = value.trim();
    if (val === '') return '';
    var result = val.substr(0, 3).replace(/\D/, '');
    return Number(result) > 120 ? 120 : result;
  }
  function updateClouds(_, element) {
    var height, coverage, types;
    var _this$elements = this.elements,
      clouds_1 = _this$elements.clouds_1,
      clouds_1_coverage = _this$elements.clouds_1_coverage,
      clouds_1_height = _this$elements.clouds_1_height,
      clouds_1_type = _this$elements.clouds_1_type;
    var _this$elements2 = this.elements,
      clouds_2 = _this$elements2.clouds_2,
      clouds_2_coverage = _this$elements2.clouds_2_coverage,
      clouds_2_height = _this$elements2.clouds_2_height,
      clouds_2_type = _this$elements2.clouds_2_type;
    var _this$elements3 = this.elements,
      clouds_3 = _this$elements3.clouds_3,
      clouds_3_coverage = _this$elements3.clouds_3_coverage,
      clouds_3_height = _this$elements3.clouds_3_height,
      clouds_3_type = _this$elements3.clouds_3_type;
    var _this$elements4 = this.elements,
      clouds_4 = _this$elements4.clouds_4,
      clouds_4_coverage = _this$elements4.clouds_4_coverage,
      clouds_4_height = _this$elements4.clouds_4_height,
      clouds_4_type = _this$elements4.clouds_4_type;
    if (clouds_1 === element || clouds_1_coverage === element || clouds_1_height === element || Array.from(clouds_1_type).includes(element)) {
      height = clouds_1.value.trim();
      coverage = clouds_1_coverage.value;
      types = clouds_1_type;
    } else if (clouds_2 === element || clouds_2_coverage === element || clouds_2_height === element || Array.from(clouds_2_type).includes(element)) {
      height = clouds_2.value.trim();
      coverage = clouds_2_coverage.value;
      types = clouds_2_type;
    } else if (clouds_3 === element || clouds_3_coverage === element || clouds_3_height === element || Array.from(clouds_3_type).includes(element)) {
      height = clouds_3.value.trim();
      coverage = clouds_3_coverage.value;
      types = clouds_3_type;
    } else if (clouds_4 === element || clouds_4_coverage === element || clouds_4_height === element || Array.from(clouds_4_type).includes(element)) {
      height = clouds_4.value.trim();
      coverage = clouds_4_coverage.value;
      types = clouds_4_type;
    } else throw new TypeError("Unable to find the required cloud layer");
    if (height === '') return '';
    var result = '';
    result += coverage;
    result += height.padStart(3, '0');
    var _iterator = _createForOfIteratorHelper(types),
      _step;
    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        var type = _step.value;
        if (type.checked) {
          result += type.value;
        }
      }
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
    return result;
  }
  function auxiliaryClouds1(value) {
    var height = this.elements.clouds_1_height;
    height.value = value === '' ? -1 : +value;
  }
  function auxiliaryClouds1Height(value) {
    var clouds_1 = this.elements.clouds_1;
    clouds_1.value = +value === -1 ? '' : value;
  }
  function auxiliaryClouds1Coverage(value) {
    var _this$elements5 = this.elements,
      height = _this$elements5.clouds_1,
      range = _this$elements5.clouds_1_height,
      types = _this$elements5.clouds_1_type;
    if (value === 'NCD') {
      height.value = '';
      range.value = -1;
      types.forEach(function (type) {
        return type.checked = false;
      });
    }
  }
  function auxiliaryClouds1Type(value, element) {
    var types = this.elements.clouds_1_type;
    var _iterator2 = _createForOfIteratorHelper(types),
      _step2;
    try {
      for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
        var type = _step2.value;
        if (type !== element) {
          type.checked = false;
        }
      }
    } catch (err) {
      _iterator2.e(err);
    } finally {
      _iterator2.f();
    }
  }
  function auxiliaryClouds2(value) {
    var height = this.elements.clouds_2_height;
    height.value = value === '' ? -1 : +value;
  }
  function auxiliaryClouds2Height(value) {
    var clouds_2 = this.elements.clouds_2;
    clouds_2.value = +value === -1 ? '' : value;
  }
  function auxiliaryClouds2Coverage(value) {
    var _this$elements7 = this.elements,
      height = _this$elements7.clouds_2,
      range = _this$elements7.clouds_2_height,
      types = _this$elements7.clouds_2_type;
    if (value === 'NCD') {
      height.value = '';
      range.value = -1;
      types.forEach(function (type) {
        return type.checked = false;
      });
    }
  }
  function auxiliaryClouds2Type(value, element) {
    var types = this.elements.clouds_2_type;
    var _iterator4 = _createForOfIteratorHelper(types),
      _step4;
    try {
      for (_iterator4.s(); !(_step4 = _iterator4.n()).done;) {
        var type = _step4.value;
        if (type !== element) {
          type.checked = false;
        }
      }
    } catch (err) {
      _iterator4.e(err);
    } finally {
      _iterator4.f();
    }
  }
  function auxiliaryClouds3(value) {
    var height = this.elements.clouds_3_height;
    height.value = value === '' ? -1 : +value;
  }
  function auxiliaryClouds3Height(value) {
    var clouds_3 = this.elements.clouds_3;
    clouds_3.value = +value === -1 ? '' : value;
  }
  function auxiliaryClouds3Coverage(value) {
    var _this$elements9 = this.elements,
      height = _this$elements9.clouds_3,
      range = _this$elements9.clouds_3_height,
      types = _this$elements9.clouds_3_type;
    if (value === 'NCD') {
      height.value = '';
      range.value = -1;
      types.forEach(function (type) {
        return type.checked = false;
      });
    }
  }
  function auxiliaryClouds3Type(value, element) {
    var types = this.elements.clouds_3_type;
    var _iterator6 = _createForOfIteratorHelper(types),
      _step6;
    try {
      for (_iterator6.s(); !(_step6 = _iterator6.n()).done;) {
        var type = _step6.value;
        if (type !== element) {
          type.checked = false;
        }
      }
    } catch (err) {
      _iterator6.e(err);
    } finally {
      _iterator6.f();
    }
  }
  function auxiliaryClouds4(value) {
    var height = this.elements.clouds_4_height;
    height.value = value === '' ? -1 : +value;
  }
  function auxiliaryClouds4Height(value) {
    var clouds_4 = this.elements.clouds_4;
    clouds_4.value = +value === -1 ? '' : value;
  }
  function auxiliaryClouds4Coverage(value) {
    var _this$elements0 = this.elements,
      height = _this$elements0.clouds_4,
      range = _this$elements0.clouds_4_height,
      types = _this$elements0.clouds_4_type;
    if (value === 'NCD') {
      height.value = '';
      range.value = -1;
      types.forEach(function (type) {
        return type.checked = false;
      });
    }
  }
  function auxiliaryClouds4Type(value, element) {
    var types = this.elements.clouds_4_type;
    var _iterator7 = _createForOfIteratorHelper(types),
      _step7;
    try {
      for (_iterator7.s(); !(_step7 = _iterator7.n()).done;) {
        var type = _step7.value;
        if (type !== element) {
          type.checked = false;
        }
      }
    } catch (err) {
      _iterator7.e(err);
    } finally {
      _iterator7.f();
    }
  }
  function clouds (form) {
    form.addHandler('clouds_1', {
      element: 'clouds_1',
      format: formatClouds,
      auxiliary: auxiliaryClouds1,
      update: updateClouds
    });
    form.addHandler('clouds_1', {
      element: 'clouds_1_height',
      auxiliary: auxiliaryClouds1Height,
      update: updateClouds
    });
    form.addHandler('clouds_1', {
      element: 'clouds_1_coverage',
      auxiliary: auxiliaryClouds1Coverage,
      update: updateClouds
    });
    form.addHandler('clouds_1', {
      element: 'clouds_1_type',
      auxiliary: auxiliaryClouds1Type,
      update: updateClouds
    });
    form.addHandler('clouds_2', {
      element: 'clouds_2',
      format: formatClouds,
      auxiliary: auxiliaryClouds2,
      update: updateClouds
    });
    form.addHandler('clouds_2', {
      element: 'clouds_2_height',
      auxiliary: auxiliaryClouds2Height,
      update: updateClouds
    });
    form.addHandler('clouds_2', {
      element: 'clouds_2_coverage',
      auxiliary: auxiliaryClouds2Coverage,
      update: updateClouds
    });
    form.addHandler('clouds_2', {
      element: 'clouds_2_type',
      auxiliary: auxiliaryClouds2Type,
      update: updateClouds
    });
    form.addHandler('clouds_3', {
      element: 'clouds_3',
      format: formatClouds,
      auxiliary: auxiliaryClouds3,
      update: updateClouds
    });
    form.addHandler('clouds_3', {
      element: 'clouds_3_height',
      auxiliary: auxiliaryClouds3Height,
      update: updateClouds
    });
    form.addHandler('clouds_3', {
      element: 'clouds_3_coverage',
      auxiliary: auxiliaryClouds3Coverage,
      update: updateClouds
    });
    form.addHandler('clouds_3', {
      element: 'clouds_3_type',
      auxiliary: auxiliaryClouds3Type,
      update: updateClouds
    });
    form.addHandler('clouds_4', {
      element: 'clouds_4',
      format: formatClouds,
      auxiliary: auxiliaryClouds4,
      update: updateClouds
    });
    form.addHandler('clouds_4', {
      element: 'clouds_4_height',
      auxiliary: auxiliaryClouds4Height,
      update: updateClouds
    });
    form.addHandler('clouds_4', {
      element: 'clouds_4_coverage',
      auxiliary: auxiliaryClouds4Coverage,
      update: updateClouds
    });
    form.addHandler('clouds_4', {
      element: 'clouds_4_type',
      auxiliary: auxiliaryClouds4Type,
      update: updateClouds
    });
  }

  // file: src/components/metar/temperature/temperature.mjs

  function formatTemperature(value) {
    // @todo fix bug -0

    value = value.trim();
    if (value === '-') return value;
    if (value === '' || isNaN(value)) return '';
    var number = +value.match(/-?\d?\d?/);
    if (isNaN(number) && value.startsWith('-')) return '-';
    return isNaN(number) ? '' : number < -80 ? -80 : number > 80 ? 80 : number;
  }
  function updateTemperature() {
    var _this$elements = this.elements,
      temperature = _this$elements.temperature,
      dew_point = _this$elements.dew_point;
    if (['', '-'].includes(temperature.value) && ['', '-'].includes(dew_point.value)) {
      return '';
    }
    var temperature_value = isNaN(+temperature.value) ? '' : temperature.value;
    var dew_point_value = isNaN(+dew_point.value) ? '' : dew_point.value;
    var result = '';
    result += temperature_value === '' ? '/' : temperature_value.replace('-', 'M');
    result += '/';
    result += dew_point_value === '' ? '/' : dew_point_value.replace('-', 'M');
    return result;
  }
  function auxiliaryTemperature(value, element) {
    var _this$elements2 = this.elements,
      temperature = _this$elements2.temperature,
      dew_point = _this$elements2.dew_point;
    var _this$elements3 = this.elements,
      temperature_range = _this$elements3.temperature_range,
      dew_point_range = _this$elements3.dew_point_range;
    var _this$elements4 = this.elements,
      temperature_minus = _this$elements4.temperature_minus,
      dew_point_minus = _this$elements4.dew_point_minus;
    var linkedElement = null;
    var minusElement = null;
    switch (element) {
      case temperature:
        linkedElement = temperature_range;
        minusElement = temperature_minus;
        break;
      case dew_point:
        linkedElement = dew_point_range;
        minusElement = dew_point_minus;
        break;
      default:
        throw new TypeError("unknown element");
    }
    if (!(linkedElement instanceof HTMLInputElement)) {
      throw new TypeError('not an input element');
    }
    if (!(minusElement instanceof HTMLInputElement)) {
      throw new TypeError("minusElement is not a proper HTMLInputElement");
    }
    linkedElement.value = value === '' || value === '-' ? -1 : Math.abs(value);
    minusElement.checked = value.startsWith('-');
  }
  function auxiliaryTemperatureRange(value, element) {
    var _minusElement;
    var _this$elements5 = this.elements,
      temperature = _this$elements5.temperature,
      dew_point = _this$elements5.dew_point;
    var _this$elements6 = this.elements,
      temperature_range = _this$elements6.temperature_range,
      dew_point_range = _this$elements6.dew_point_range;
    var _this$elements7 = this.elements,
      temperature_minus = _this$elements7.temperature_minus,
      dew_point_minus = _this$elements7.dew_point_minus;
    var linkedElement = '';
    var minusElement = '';
    switch (element) {
      case temperature_range:
        linkedElement = temperature;
        minusElement = temperature_minus;
        break;
      case dew_point_range:
        linkedElement = dew_point;
        minusElement = dew_point_minus;
        break;
      default:
        throw new Error("unknown element");
    }
    if (!(linkedElement instanceof HTMLInputElement) || !('value' in linkedElement)) {
      throw new TypeError("linkedElement must be a typeof HTMLInputElement(text), but got ".concat(_typeof(linkedElement)));
    }
    if (!(minusElement instanceof HTMLInputElement) || ((_minusElement = minusElement) === null || _minusElement === void 0 ? void 0 : _minusElement.type) !== 'checkbox') {
      throw new TypeError("minusElement must be a typeof HTMLInputElement(checkbox), but got ".concat(_typeof(minusElement)));
    }

    //if (value < 0) {
    //  linkedElement.value = ''
    //} else {
    //  if (minusElement.checked)
    //    value = `-${value}`
    //  linkedElement.value = value
    //}

    linkedElement.value = value < 0 ? '' : minusElement.checked ? "-".concat(value) : /* default */value;
  }
  function temperature (form) {
    form.addHandler('temperature', {
      element: 'temperature',
      format: formatTemperature,
      auxiliary: auxiliaryTemperature,
      update: updateTemperature
    });
    form.addHandler('temperature', {
      element: 'dew_point',
      format: formatTemperature,
      auxiliary: auxiliaryTemperature,
      update: updateTemperature
    });
    form.addHandler('temperature', {
      element: 'temperature_range',
      //auxiliary(value) { // @todo auxiliaryTemperatureRange
      //  const { temperature } = this.elements
      //  temperature.value = value < 0 ? '' : value
      //},
      auxiliary: auxiliaryTemperatureRange,
      update: updateTemperature
    });
    form.addHandler('temperature', {
      element: 'dew_point_range',
      //auxiliary(value) { // @todo auxiliaryTemperatureRange
      //  const { dew_point } = this.elements
      //  dew_point.value = value < 0 ? '' : value
      //},
      auxiliary: auxiliaryTemperatureRange,
      update: updateTemperature
    });
    function auxiliaryTemperatureMinus(_, element) {
      var _this$elements8 = this.elements,
        temperature = _this$elements8.temperature,
        dew_point = _this$elements8.dew_point;
      var linkedElement = '';
      var value = '';
      if (element.checked) value += '-';
      switch (element.name) {
        case 'temperature_minus':
          linkedElement = temperature;
          break;
        case 'dew_point_minus':
          linkedElement = dew_point;
          break;
        default:
          throw TypeError("unknown element name");
      }
      value += linkedElement.value.replace(/\D/, '');
      if (!isNaN(value)) linkedElement.value = value;
    }
    form.addHandler('temperature', {
      element: 'temperature_minus',
      auxiliary: auxiliaryTemperatureMinus,
      update: updateTemperature
    });
    form.addHandler('temperature', {
      element: 'dew_point_minus',
      auxiliary: auxiliaryTemperatureMinus,
      update: updateTemperature
    });
  }

  // file: components/metar/pressure/pressure.mjs

  // @todo
  //function auxiliary(value, element) {
  //
  //  const { pressure, pressure_range: range } = this.elements
  //
  //  switch (element) {
  //
  //    case pressure:
  //      range.value = pressure.value
  //      break
  //
  //    case range:
  //      pressure.value = range.value
  //      break
  //  }
  //
  //}

  function update$1(value, element) {
    var result = value < 900 || value > 1100 ? '' : value;
    return result === '' ? '' : "Q".concat(result);
  }
  function pressure (form) {
    form.addHandler('pressure', {
      format: function format(value) {
        var result = value.trim().replace(/\D/, '').substring(0, 4);
        if (result === '') return '';
        return Number(result) > 1100 ? 1100 : result;
      },
      auxiliary: function auxiliary(value) {
        var range = this.elements.pressure_range;
        range.value = value === '' || Number(value) < 900 ? 899 : value;
      },
      update: update$1
    });
    form.addHandler('pressure', {
      element: 'pressure_range',
      auxiliary: function auxiliary(value) {
        var pressure = this.elements.pressure;
        pressure.value = Number(value) === 899 ? '' : value;
      },
      update: update$1
    });
  }

  // file: src/components/metar/rmk/rmk.mjs

  function rmk (form) {
    form.addHandler('rmk', {
      init: init,
      format: format,
      update: update
    });
  }
  function init() {
    this.result.set('rmk', '=');
  }
  function format(value) {
    return value.trim().toUpperCase();
  }
  function update() {
    var value = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : '';
    return value + '=';
  }

  // src/components/metar/metar.mjs

  var METAR_COMPONENTS = {
    type: type,
    icao: icao,
    cor: cor,
    datetime: datetime,
    nil: nil,
    auto: auto,
    wind: wind,
    //wind_vrb,
    cavok: cavok,
    visibility: visibility,
    visibility_minimal: visibility_minimal,
    weather: weather,
    visibility_vertical: visibility_vertical,
    clouds: clouds,
    temperature: temperature,
    pressure: pressure,
    //  trend,
    rmk: rmk
  };

  // file: src/index.mjs

  //import TAF_COMPONENTS   from './components/taf/taf.mjs'

  // Константы

  var METAR_FORM_SELECTOR = '#amtk_wre_metar';

  // Списки названий компонентов,
  // по которым будут искаться и загружаться в формы
  // необходимые для них компоненты

  var METAR_COMPONENTS_NAMES = ['type', 'cor', 'icao', 'datetime', 'nil', 'auto', 'wind',
  //'wind_vrb',
  'cavok', 'visibility', 'visibility_minimal', 'weather', 'visibility_vertical', 'clouds', 'temperature', 'pressure',
  //'trend',
  'rmk'];

  //const TAF_COMPONENTS_NAMES = [
  //  // type,
  //  // ...
  //]

  // Создание форм
  // и отрисовка компонентов в них

  var METAR_FORM = new _default(METAR_FORM_SELECTOR, METAR_COMPONENTS, METAR_COMPONENTS_NAMES);

  //const TAF_FORM = new FormManager(
  //  TAF_FORM_SELECTOR,
  //  TAF_COMPONENTS,
  //  TAF_COMPONENTS_NAMES
  //)

  // Загрузка логики приложения

  loadForm(METAR_FORM);
  //loadForm(TAF_FORM)

  // Логика меню

  //const entries = [
  //  {
  //    btn: document.querySelector('#amtk_wre_menu_btn_metar'),
  //    form: document.querySelector('#amtk_wre_metar'),
  //  },
  //  {
  //    btn: document.querySelector('#amtk_wre_menu_btn_taf'),
  //    form: document.querySelector('#amtk_wre_taf'),
  //  },
  //]
  //
  //for (const {btn, form} of entries) {
  //  btn.addEventListener('click', e => {
  //    entries.forEach(entry => {
  //      if (entry.btn === e.target) {
  //        entry.form.classList.remove('amtk_wre_hidden')
  //        entry.btn.classList.add('amtk_wre_active')
  //      } else {
  //        entry.form.classList.add('amtk_wre_hidden')
  //        entry.btn.classList.remove('amtk_wre_active')
  //      }
  //    })
  //  })
  //}

})();
